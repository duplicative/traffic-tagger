# MongoDB Configuration
MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=password123

# MongoDB Connection URI for the application
MONGO_URI=mongodb://root:password123@database:27017/http_tagger?authSource=admin

# API Port (if needed, but default FastAPI is 8000)
# API_PORT=8000
